import { ProductnamePipePipe } from './productname-pipe.pipe';

describe('ProductnamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductnamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
